# API
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic-settings==2.7.1

# persistence
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.20.0

# speech
faster-whisper==1.1.1
librosa==0.10.2.post1
soundfile==0.12.1
# kokoro-onnx needs numpy>=2.0.2; the pipeline (whisper/librosa/numba) was
# re-verified end to end against 2.5.3 when this was bumped from 1.26.4.
numpy==2.5.3

# resume parsing
pdfplumber==0.11.4
python-docx==1.1.2

# text-to-speech
kokoro-onnx==0.6.1

# LLM (DeepSeek) — question generation and report judgment, text only
httpx==0.28.1
